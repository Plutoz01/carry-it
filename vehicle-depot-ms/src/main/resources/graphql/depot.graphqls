extend type Query {
    getAllDepot(page: Page): PagedDepots!
    getDepotById(id: ID!): Depot
}

extend type Mutation {
    createDepot(input: CreateDepotInput!): Depot!
}

type Depot {
    id: ID!
    name: String!
    vehicles: [Vehicle]!
    vehicleCount: Int!
}

type PagedDepots {
    items: [Depot]!
    pageInfo: PageInfo!
}

input CreateDepotInput {
    name: String!
}
